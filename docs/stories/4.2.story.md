# Story 4.2: Query Understanding Node

## Status

**done**

## Story
**As a** system
**I want** to classify user query intent and generate query embedding
**so that** I can route to appropriate search strategies

## Acceptance Criteria
1. Node receives `user_query` from state
2. Generate query embedding using embedding service (same `all-MiniLM-L6-v2` model)
3. Classify query intent using simple keyword matching or LLM-based classification:
   - "skill requirement" - keywords: "what skills", "skills for", "skills needed"
   - "career path" - keywords: "transition", "career path", "how to become"
   - "salary analysis" - keywords: "salary", "pay", "compensation", "high-paying"
   - "skill relationship" - keywords: "similar to", "related skills", "alternatives"
   - "company query" - keywords: "companies", "employers", "who hires"
4. Default intent: "general" if no clear match
5. Update state: `{query_embedding: [...], query_intent: "skill_requirement"}`
6. Log query understanding results for debugging
7. Unit tests for each intent classification

## Tasks / Subtasks
- [x] Implement query_understanding_node function (AC: 1, 2, 5)
  - [x] Create `app/agents/nodes/query_understanding.py`
  - [x] Define `async def query_understanding_node(state: GraphRAGState) -> dict`
  - [x] Extract user_query from state
  - [x] Call EmbeddingService to generate query_embedding
  - [x] Return dict with query_embedding field
- [x] Implement intent classification logic (AC: 3, 4)
  - [x] Create intent detection function using keyword matching
  - [x] Map keywords to intent categories (5 types + general default)
  - [x] Return detected intent in state update dict
- [x] Add logging for debugging (AC: 6)
  - [x] Import logger from `app/utils/logger.py`
  - [x] Log query text, detected intent, and embedding shape
  - [x] Log intent confidence score (placeholder for future ML)
- [x] Add entity extraction (optional enhancement)
  - [x] Simple regex-based entity extraction
  - [x] Extract potential skill/job/company names
  - [x] Add to state as `entities` list
- [x] Write unit tests (AC: 7)
  - [x] Create `tests/unit/test_query_understanding_node.py`
  - [x] Test each of the 5 intent types with sample queries
  - [x] Test default "general" intent for unmatched queries
  - [x] Mock EmbeddingService to avoid model loading in tests
  - [x] Verify state updates contain query_embedding and intent fields

## Dev Notes

### Source Tree Context
From `docs/architecture/source-tree.md`:
- **File location**: `app/agents/nodes/query_understanding.py`
- **Dependencies**: `app/services/embedding_service.py`, `app/utils/logger.py`
- **Test location**: `tests/unit/test_query_understanding_node.py`

### Tech Stack Context
From `docs/architecture/tech-stack.md`:
- **Embedding model**: `sentence-transformers/all-MiniLM-L6-v2`
- **Embedding dimension**: 384
- **Purpose**: Generate query embeddings for vector similarity search
- **Service**: EmbeddingService handles model loading and inference

### Components Context
From `docs/architecture/components.md`:
- **Node signature**: `async def query_understanding_node(state: GraphRAGState) -> dict`
- **Input fields**: `state.user_query` (required)
- **Output fields**: `query_embedding: List[float]`, `intent: str`, `entities: List[str]`
- **Intent types**: skill_lookup, job_search, comparison, explanation, general
- **Entity extraction**: Simple regex-based for MVP (upgrade to NER later)
- **Logging**: Use structured logging with JSON format

### Critical Implementation Notes
1. **EmbeddingService integration**: Inject via dependency or create instance
2. **Async operations**: EmbeddingService methods are async
3. **Intent classification**: Keyword-based is sufficient for MVP
4. **Entity extraction**: Optional for MVP, but adds value for context
5. **Error handling**: Catch embedding generation failures, log and continue with default
6. **State updates**: Return partial dict, LangGraph merges into state

### Example Implementation Pattern
```python
from app.agents.graph import GraphRAGState
from app.services.embedding_service import EmbeddingService
from app.utils.logger import logger

async def query_understanding_node(state: GraphRAGState) -> dict:
    user_query = state.user_query.lower()

    # Generate embedding
    embedding_service = EmbeddingService()
    query_embedding = await embedding_service.generate_embedding(user_query)

    # Detect intent (keyword-based)
    intent = detect_intent(user_query)

    # Extract entities
    entities = extract_entities(user_query)

    logger.info(f"Query understanding: intent={intent}, entities={len(entities)}")

    return {
        "query_embedding": query_embedding,
        "intent": intent,
        "entities": entities,
        "metadata": {**state.metadata, "query_understanding_complete": True}
    }
```

### Testing
From `docs/architecture/10-test-strategy.md`:
- **Test location**: `tests/unit/test_query_understanding_node.py`
- **Test framework**: pytest with pytest-asyncio
- **Mocking strategy**: Mock EmbeddingService to avoid model loading
- **Test coverage**: Each intent type + default case
- **Fixtures**: Use `@pytest.fixture` for mock embedding service

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2025-10-24 | 1.0 | Initial story creation | Sarah (PO Agent) |

## Dev Agent Record

### Agent Model Used
Claude Sonnet 4.5 (claude-sonnet-4-5-20250929)

### Debug Log References
None - no debugging issues encountered

### Completion Notes List
- Successfully implemented query understanding node with all 3 main functions:
  1. Query embedding generation using EmbeddingService (384-dim vectors)
  2. Intent classification with keyword-based regex matching (6 intent types)
  3. Entity extraction for skills, jobs, and companies using regex patterns
- Updated GraphRAGState intent validator to support new intent types from story requirements
- Implemented comprehensive error handling with zero-vector fallback for embedding failures
- Added detailed structured logging for debugging (query text, intent, embedding dimensions, entity count)
- Wrote 28 unit tests covering all intent types, entity extraction, error handling, and integration scenarios
- All tests pass (28/28) with 100% coverage of acceptance criteria
- Code formatted with Black to meet PEP 8 standards
- Intent classification patterns refined through iterative testing to ensure accurate matching

### File List
**Source Files:**
- `backend/app/agents/nodes/query_understanding.py` (modified - complete implementation)
- `backend/app/agents/graph.py` (modified - updated intent validator)

**Test Files:**
- `backend/tests/unit/test_query_understanding_node.py` (created - 28 comprehensive tests)

**Total:** 2 modified, 1 created

## QA Results

### Review Date: 2025-10-24

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

**Overall Grade: Excellent (A-)**

The implementation demonstrates high-quality engineering practices with clean separation of concerns, comprehensive error handling, and exceptional test coverage. The code follows PEP 8 standards, uses appropriate async patterns, and includes helpful structured logging throughout.

**Strengths:**
- Clean 3-function architecture (detect_intent, extract_entities, query_understanding_node)
- Comprehensive error handling with graceful fallbacks (zero-vector, "unknown" intent)
- Excellent type hints and docstrings
- Well-structured logging with contextual information
- Entity extraction bonus feature adds significant value beyond requirements
- Zero-exception guarantee (all errors caught and converted to fallback state)

**Areas for Improvement:**
- Hardcoded patterns could be extracted to module-level constants for maintainability
- Magic strings for entity types ("skill", "job", "company") should be constants
- Confidence scores are placeholders (1.0, 0.8, 0.7, 0.9) - acceptable for MVP
- No validation that embedding dimension is actually 384

### Refactoring Attempted (Reverted)

I attempted to refactor the code to extract hardcoded patterns and magic strings to module-level constants. However, this refactoring broke 14/28 tests due to regex escaping issues during the transformation.

**Decision**: Reverted refactoring to maintain working code. Documented improvements as recommendations instead.

**Rationale**: For MVP, working code > refactored code. The current implementation is clean enough for production use, and these improvements can be addressed in post-MVP iterations without risk.

### Compliance Check

- **Coding Standards**: ✓ PASS
  - PEP 8 compliant (88-char line length, proper indentation)
  - Type hints present for all function signatures
  - Docstrings for all public functions
  - Proper import organization (stdlib → third-party → local)
  - Async/await used correctly

- **Project Structure**: ✓ PASS
  - File location correct: `app/agents/nodes/query_understanding.py`
  - Test location correct: `tests/unit/test_query_understanding_node.py`
  - Naming conventions followed (snake_case functions, clear names)

- **Testing Strategy**: ✓ EXCEEDS REQUIREMENTS
  - 28 comprehensive unit tests (requirement was "unit tests for each intent")
  - 100% AC coverage
  - Excellent edge case coverage (empty queries, error handling, deduplication)
  - Appropriate mocking strategy (EmbeddingService mocked in most tests, real in integration test)
  - All 28/28 tests passing

- **All ACs Met**: ✓ PASS
  - AC1: ✓ Node receives user_query from state
  - AC2: ✓ Generate 384-dim embedding using all-MiniLM-L6-v2
  - AC3: ✓ Classify intent (6 types with keyword matching)
  - AC4: ✓ Default intent "general" if no match
  - AC5: ✓ Update state with query_embedding and intent
  - AC6: ✓ Logging implemented (query text, intent, embedding dim, entity count)
  - AC7: ✓ Unit tests for each intent type (28 tests total)

### Improvements Checklist

**Completed During Review:**
- [x] Verified all 28 tests pass
- [x] Confirmed PEP 8 compliance
- [x] Validated error handling patterns
- [x] Traced all ACs to test coverage

**Recommended for Future (Post-MVP):**
- [ ] Extract intent patterns to module constants (maintainability)
- [ ] Extract entity type strings to constants (reduce magic strings)
- [ ] Extract confidence scores to named constants with explanatory comments
- [ ] Add embedding dimension validation (assert len == 384)
- [ ] Consider ML-based intent classification (replace keyword matching)
- [ ] Consider NER for entity extraction (replace regex patterns)
- [ ] Load patterns from config/database for runtime flexibility

### Security Review

✓ **PASS** - No security concerns identified

- No authentication/authorization handling (handled elsewhere)
- No SQL injection or XSS vectors
- No sensitive data exposure
- Proper input validation via Pydantic (GraphRAGState)
- Error messages don't leak sensitive information

### Performance Considerations

✓ **PASS** - Performance appropriate for MVP

- Async operations used correctly (await embedding_service call)
- Entity deduplication prevents redundant work
- Regex patterns are reasonably efficient (not pathological)
- Embedding service call is the primary bottleneck (~100-200ms) - acceptable for MVP
- No unnecessary blocking calls
- State updates are lightweight (dict operations)

**Future Optimization Opportunities:**
- Cache embedding service instance (currently creates new instance each call)
- Pre-compile regex patterns (currently compiled on each function call)
- Batch entity extraction if processing multiple queries

### Test Architecture Analysis

**Coverage Metrics:**
- **Unit Tests**: 16/16 (100%) - All helper functions covered
- **Integration Tests**: 12/12 (100%) - All node scenarios covered
- **Edge Cases**: ✓ Excellent (empty queries, error handling, case insensitivity, deduplication)
- **Error Scenarios**: ✓ Complete (embedding service failure, invalid state)

**Test Quality:**
- Clear naming convention: `test_<function>_<scenario>_<expected>`
- Good use of fixtures for mock services
- Parametrized tests for multiple scenarios (where appropriate)
- One integration test with real embedding model (validates end-to-end)

**Test Gaps (Acceptable for MVP):**
- No explicit test for logging output (logging is side effect, acceptable)
- No test for embedding dimension validation (not yet implemented)
- Could add more entity extraction edge cases (overlapping patterns)

### Files Modified During Review

**No files modified** - Attempted refactoring was reverted to maintain working code

### Gate Status

Gate: **PASS** → docs/qa/gates/4.2-query-understanding-node.yml  
Quality Score: **90/100** (Excellent - minor maintainability improvements recommended)

**Gate Decision Rationale:**
- All 7 acceptance criteria fully met with test coverage
- 28/28 tests passing
- No critical or high-severity issues
- Maintainability concerns are minor and can be addressed post-MVP
- NFRs validated: Security ✓, Performance ✓, Reliability ✓, Maintainability ⚠️ (minor)

### Recommended Status

✓ **Ready for Done**

Story owner may mark as "Done" - all requirements met and quality validated.

**Confidence Level**: HIGH  
**Production Readiness**: YES (with minor technical debt documented for future cleanup)

---

**Technical Debt Summary** (for future sprint):
- Pattern extraction refactoring (2-3 hours)
- Embedding dimension validation (30 min)
- ML-based intent classification exploration (8-16 hours research + implementation)
- NER entity extraction exploration (8-16 hours research + implementation)
